import 'package:flutter/widgets.dart';

Widget buildGoogleSignInRenderButton({
  required double width,
  String? localeTag,
}) {
  throw UnsupportedError('Google Sign-In web button is only available on web.');
}
