/// One visual option in the detail-page sprite carousel.
class const PokemonSpriteVariant({
  required final String imageUrl,
  required final int pokemonId,
  required final String labelKey,
  final bool isShiny = false,
});
